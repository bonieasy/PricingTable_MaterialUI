import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {},
    typography: {
        fontFamily: "'Open Sans', sans-serif",
        h1: {
            fontSize: 36,
        },
        h2: {
            fontSize: 18,
        }
    }, 
})